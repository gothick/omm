version: '3.4'

services:
  web:
    build: .
    ports:
        - 8192:80
    depends_on:
      - "docker-database"
      - "beanstalkd"
      - "elasticsearch"

###> doctrine/doctrine-bundle ###
###< doctrine/doctrine-bundle ###
