{% extends 'base.html.twig' %}

{% block title %}Edit Settings{% endblock %}

{% block body %}
    <h1>Edit Settings</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        {# <button class="btn">{{ button_label|default('Save') }}</button> #}
    {{ form_end(form) }}

    <a href="{{ path('settings_index') }}">back to settings page</a>
{% endblock %}
