<form method="post" action="{{ path('admin_wanders_delete', {'id': wander.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wander.id) }}">
    <button class="btn btn-danger">Delete Wander</button>
</form>
