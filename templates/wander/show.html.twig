{% extends 'base.html.twig' %}

{% block title %}Hello WanderController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello! ✅</h1>

    I'd be showing wander {{ wander.id }} if I'd been written properly yet.
    <h2>{{ wander.title }}</h2>
    <p>{{ wander.description }}</p>
    <p>{{ wander.gpxFilename }}</p>
    <p><a href="{{ asset('uploads/gpx/' ~ wander.gpxFilename) }}">View GPX file</a></p>
    <p>From {{ wander.startTime|date('r') }} to {{ wander.endTime|date('r') }}
</div>
{% endblock %}
